# v67 全面审计记录

## 2026-08-21 生产首页首屏

在 `https://nexuschat.best/` 连续观察两次后，页面先显示品牌启动遮罩，随后仍停留在消息列表骨架屏，未出现可交互内容。该现象需要继续核对认证状态、入口路由及骨架屏隐藏条件，尚未在本记录中归因为单一代码缺陷。

后续在生产页运行 DOM 检查时，页面加载已超过 48 秒，`#splash-screen` 和 `#skeleton-screen` 均已移除，但 `#root` 的子元素数量仍为 0；入口模块 `/assets/index-CsD56Te9.js` 返回 200、`text/javascript`、约 43 KB。该证据表明生产端的 React 应用未成功挂载，而不是单纯骨架屏没有隐藏。

入口模块文本包含 `createRoot(...).render(...)` 与 `window.__APP_RENDER_START__` 标记，但在生产页面上这些标记均未写入，且 `#root` 仍为空。入口模块本身已被请求，说明模块在其自身求值之前（例如静态依赖加载或执行）即停止，需继续检查生产构建的静态依赖链与响应头。

作为对照，当前预览环境已正常挂载 React 并完整渲染首页。因此该故障不由最新本地应用代码的常规渲染路径引起，优先检查生产域名的静态资源版本、缓存与 Service Worker 控制状态。

生产域名当前由 `https://nexuschat.best/sw.js` 控制。该页面的 `__APP_RENDER_START__`、`__APP_RENDER_DONE__` 和 `__APP_RENDER_ERROR__` 均为空，`#root` 子元素为 0。该 Service Worker 或其缓存更新路径是当前首要排查对象。

## 2026-08-21 服务恢复验证

全量构建后开发服务曾因高内存压力导致 esbuild 子进程退出；停止残留的 `tsc --noEmit --watch` 与 esbuild 进程、释放页缓存并重启服务后，预览首页已正常完成 React 挂载和首屏渲染。该问题是沙箱构建资源竞争，已恢复；生产端仍待随本轮包含 `dist` 的发布版本验证。
