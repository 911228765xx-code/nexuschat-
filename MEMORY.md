# Island Farm Implementation Notes

- 游戏入口为 `/app/island`，放在现有 AppLayout 之内，并从发现页提供入口，避免改变五个主 Tab 的信息架构。
- 当前项目已使用 BitChat 的 `users.nnBalance` 作为 BIT 余额来源；旧代码中部分命名仍为 NN，游戏界面统一对用户显示为 BIT。
- 首期不会创建玩家市场，也不会触发链上或真实资产结算；这些能力需要单独的安全、合规和风控设计后才可开放。
- 视觉基准使用已上传的项目静态资源 `/manus-storage/island-farm-reference_7519565a.png`，可随 WebDev 发布版本稳定加载。
- 开发预览可通过 `/app/island?demo=1` 验证 Babylon 场景；该分支仅在 Vite 开发环境生效，生产路由仍强制登录。首次 App 引导和版本提示会遮挡页面，跳过引导后画布、资产卡片和控制面板均已成功渲染。Babylon 原始坐标拾取在浏览器自动化中不稳定，因此增加了同步的无障碍/触控命中区域；浏览器已验证“农田 3”会更新选择状态和种植按钮。
- 浏览器已验证在演示模式选择农田 3 后可完成种植，页面会切换至“生长中”并显示服务器同构的倒计时状态；工坊升级已通过浏览器事件验证，会从 Lv.1 更新为 Lv.2、IT 从 216 更新为 228，且下一等级的资源要求同步刷新。生产结算仍由服务端事务负责。
- 静态资源上传后已复测页面，生成的岛屿视觉图通过项目资源地址正常加载；浏览器控制台未见游戏运行时异常，唯一记录是验证脚本第一次误用 TypeScript 断言造成的浏览器语法错误，后续标准 JavaScript 事件验证正常完成。
